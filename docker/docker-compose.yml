services:
  home-assistant:
    container_name: ha-dev
    image: ghcr.io/home-assistant/home-assistant:stable
    ports:
      - "8123:8123"
    volumes:
      - ./ha-config:/config
    restart: unless-stopped
