default_config:

# YAML mode keeps the development dashboard reproducible: it is versioned in
# ui-lovelace.yaml rather than living in the browser's storage.
lovelace:
  mode: yaml
  resources:
    # Home Assistant caches /local/ for a month. Bump ?v= to see a rebuild
    # immediately instead of hunting for a stale bundle.
    - url: /local/solar-router/solar-router-card.js?v=3
      type: module
    # Development only: bundles the fixtures so every state of the card is
    # reachable without a router on the network.
    - url: /local/solar-router/solar-router-demo.js?v=3
      type: module

logger:
  default: info
